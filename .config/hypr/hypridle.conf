# ── hypridle.conf ───────────────────────────────────────
general {
    lock_cmd = echo "locked"
    before_sleep_cmd = echo "sleeping"
    after_sleep_cmd = echo "awake"
}

listener {
    timeout = 10
    on-timeout = ~/.local/bin/screensaver.sh
    on-resume = pkill -f screensaver.sh
}

